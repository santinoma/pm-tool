import { SettingsPlaceholderPage } from "@/ui/nextelite/SettingsPlaceholderPage";

export const dynamic = "force-dynamic";

export default function AppearanceSettingsPage() {
  return (
    <SettingsPlaceholderPage
      title="Appearance"
      description="Passe das Erscheinungsbild deines Workspaces an — folgt in einem kommenden Modul."
    />
  );
}
