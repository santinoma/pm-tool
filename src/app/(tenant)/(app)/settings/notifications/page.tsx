import { SettingsPlaceholderPage } from "@/ui/nextelite/SettingsPlaceholderPage";

export const dynamic = "force-dynamic";

export default function NotificationsSettingsPage() {
  return (
    <SettingsPlaceholderPage
      title="Notifications"
      description="Passe an, welche Benachrichtigungen du in der App, per E-Mail oder über Slack erhältst — folgt in einem kommenden Modul."
    />
  );
}
