import { SettingsPlaceholderPage } from "@/ui/nextelite/SettingsPlaceholderPage";

export const dynamic = "force-dynamic";

export default function EmployeeFieldsSettingsPage() {
  return (
    <SettingsPlaceholderPage
      title="Employee fields"
      description="Richte eure Personaldaten-Felder ein — folgt in einem kommenden Modul."
    />
  );
}
