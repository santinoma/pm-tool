import { SettingsPlaceholderPage } from "@/ui/shell/SettingsPlaceholderPage";

export const dynamic = "force-dynamic";

export default function WorkflowsSettingsPage() {
  return (
    <SettingsPlaceholderPage
      title="Workflows"
      description="Erstelle und bearbeite Gruppen von Task-Status — folgt in einem kommenden Modul."
    />
  );
}
