import { SettingsPlaceholderPage } from "@/ui/shell/SettingsPlaceholderPage";

export const dynamic = "force-dynamic";

export default function ServiceTypesSettingsPage() {
  return (
    <SettingsPlaceholderPage
      title="Service types"
      description="Verwalte Leistungstypen, die dein Unternehmen euren Kunden anbietet — folgt in einem kommenden Modul."
    />
  );
}
