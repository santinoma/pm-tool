import { SettingsPlaceholderPage } from "@/ui/shell/SettingsPlaceholderPage";

export const dynamic = "force-dynamic";

export default function RecycleBinSettingsPage() {
  return (
    <SettingsPlaceholderPage
      title="Recycle bin"
      description="Finde und stelle gelöschte Elemente wieder her — folgt in einem kommenden Modul."
    />
  );
}
